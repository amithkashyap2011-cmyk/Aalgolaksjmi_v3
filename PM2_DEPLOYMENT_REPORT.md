# PM2 DEPLOYMENT REPORT

## Configuration
- `ecosystem.config.js` implemented.
- Manages `aqea-server` (Node) and `aqea-quant` (Python).
- Automatic restarts enabled with 3s/5s delays.

## Logs
- Standardized logs in `./logs/`.
- Merged logs for easier debugging.
