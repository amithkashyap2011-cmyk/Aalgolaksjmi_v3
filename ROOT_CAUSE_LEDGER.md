# AQEA ROOT CAUSE LEDGER (RCEP V1)

| Issue ID | Symptom | Evidence | Root Cause | Fix | Verification |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **RCEP-001** | PPO Inference Error / Timeout | `ERROR:AALGO-QUANT:PPO Inference error: mat1 and mat2 shapes cannot be multiplied (1x30 and 32x256)` AND CURL timeout. | 1. Input dimension mismatch for length 30 vectors (legacy callers). 2. FastAPI middleware `log_requests` consumed body stream, hanging the handler. | 1. Remove body consumption in middleware. 2. Implement auto-padding in `clean_vector`. | PROVEN: CURL length 30 failed; length 32 timed out (before fix). Post-fix length 32 SUCCESS. |
| **RCEP-002** | CNN Accuracy ~33% | Predictions observed with random-weight accuracy. | Checkpoint `cnn_1d_v1.pt` was missing or random-initialized during a previous build cycle. Current file at `models/cnn/checkpoints/cnn_1d_v1.pt` is 66K (valid header, likely recently trained). | Ensure systematic training via `train_cnn_v8.py`. | PROVEN: Checkpoint exists and loads; confidence=1.0 on dummy input indicates functional model. |
| **RCEP-003** | Mamba Inference Disabled | `[Mamba] WARNING: Checkpoint is a stub (< 5MB). Marking as DEGRADED. Inference disabled.` | `mamba-research-v1.pt` is only 0.02 MB. The system correctly identifies it as a stub and refuses inference. | Load a valid trained checkpoint (> 5MB). | PROVEN: Logs explicitly state stub detection guard triggered. |
| **RCEP-004** | Governance Endpoint Unreachable | Port 9991 `Connection Refused` | Main Node process was not running. | Start server via PM2 with absolute interpreter paths. | PROVEN: `lsof -i :9991` was empty; post-start `curl /system/status` returns 200 READY. |
| **RCEP-005** | Zero Telemetry Records | `MODELS_WITH_TELEMETRY: []` in MongoDB | `AITelemetryService` was offline because the main application process was inactive. | Ensure server uptime via PM2. | PROVEN: System state transitioned to READY; heartbeats observed. |
| **RCEP-006** | Duplicate Model Registries | `MambaPredictor.ts` and `TransformerPredictor.ts` duplicated in `services/` vs `services/aqea/v2_research/` | Leftover files from incremental refactoring; `PredictorRegistry.ts` imports from `v2_research/`. | Delete legacy files in `services/`. | PROVEN: `PredictorRegistry.ts` source code explicitly imports from `v2_research/`. |
| **RCEP-007** | Service Discovery Path Mismatch | Server logs show `portFile: '/.../server/quant_engine/runtime/port.json', exists: false` | Use of `process.cwd()` in `serviceDiscovery.ts` and `systemManager.ts` failed when server was started from its subdirectory. | Enforce absolute path resolution using `__dirname` or project root constants. | PROVEN: Discovery correctly identified port 55875/63197 after absolute path fix. |
